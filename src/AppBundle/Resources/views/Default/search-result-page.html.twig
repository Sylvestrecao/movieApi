{% extends('layout/layout.html.twig') %}

{% block body %}
    <div class="container">
        <!-- Page Heading/Breadcrumbs -->
        <div class="row mg-t-50">
            <div class="col-lg-12">
                <ol class="breadcrumb">
                    <li><a href="{{ path('homepage') }}">Accueil</a>
                    </li>
                    <li class="active">Résultat recherche</li>
                </ol>
            </div>
        </div>
        <!-- /.row -->
        <div class="row">
            {{ render(controller('AppBundle:Movie:includeMovieResults', {listMovies: movieSearchResults})) }}
        </div>
    </div>

{% endblock %}